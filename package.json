{
        "name": "@kompanie/reagenz",
        "version": "2.1.0",
        "license": "MIT",
        "main": "index.js",
        "repository": "github:kompanie-dev/reagenz",
        "type": "module",
        "files": [
                "/source"
        ],
        "scripts": {
                "build": "npx parcel build index.js --dist-dir build --no-cache",
                "start": "npx parcel demo/index.html --dist-dir build --no-cache --port 8000",
                "test": "npx parcel demo/tests.html --dist-dir build --no-cache --port 8001"
        },
        "devDependencies": {
                "parcel": "^2.11.0"
        }
}
